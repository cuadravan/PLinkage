<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:PLinkage.ViewModels"
             x:Class="PLinkage.Views.SkillProviderOffersAndApplicationsView"
             Title="Offers and Applications">

    <ScrollView Padding="20">
        <VerticalStackLayout Spacing="25">

            <!-- SECTION 1: Sent Offers (Pending) -->
            <Label Text="Sent Applications (Pending)" FontAttributes="Bold" FontSize="18" />
            <CollectionView ItemsSource="{Binding SentApplicationsPending}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" Stroke="#D9D9D9" StrokeThickness="1"
                                StrokeShape="RoundRectangle 12" Padding="14" Margin="0,0,0,10">
                            <Grid ColumnDefinitions="3*,3*,2*" VerticalOptions="Center">
                                <VerticalStackLayout>
                                    <Label Text="🔵 Offer" FontSize="10" TextColor="#2196F3" />
                                    <Label Text="{Binding ProjectName}" FontAttributes="Bold">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer
        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SkillProviderApplicationOfferViewModel}}, Path=ViewProjectCommand}" 
                                                CommandParameter="{Binding .}"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Text="{Binding FormattedRate}" FontSize="12" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1" HorizontalOptions="Center">
                                    <Label Text="{Binding ReceiverName}" FontAttributes="Bold">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer
Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SkillProviderApplicationOfferViewModel}}, Path=ViewProjectOwnerCommand}" 
                                                 CommandParameter="{Binding .}"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Text="{Binding FormattedTimeFrame}" FontSize="12" />
                                </VerticalStackLayout>
                                <Label Grid.Column="2" Text="Pending" TextColor="Gray" VerticalOptions="Center" FontAttributes="Bold" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- SECTION 2: Received Applications (Pending) -->
            <Label Text="Received Offers (Pending)" FontAttributes="Bold" FontSize="18" />
            <CollectionView ItemsSource="{Binding ReceivedOffersPending}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" Stroke="#D9D9D9" StrokeThickness="1"
                                StrokeShape="RoundRectangle 12" Padding="14" Margin="0,0,0,10">
                            <Grid ColumnDefinitions="3*,3*,2*" VerticalOptions="Center">
                                <VerticalStackLayout>
                                    <Label Text="🟠 Application" FontSize="10" TextColor="#FF9800" />
                                    <Label Text="{Binding ProjectName}" FontAttributes="Bold">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SkillProviderApplicationOfferViewModel}}, Path=ViewProjectCommand}" 
                                                 CommandParameter="{Binding .}"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Text="{Binding FormattedRate}" FontSize="12" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1" HorizontalOptions="Center">
                                    <Label Text="{Binding SenderName}" FontAttributes="Bold">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer
            Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SkillProviderApplicationOfferViewModel}}, Path=ViewProjectOwnerCommand}" 
                                                 CommandParameter="{Binding .}"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Text="{Binding FormattedTimeFrame}" FontSize="12" />
                                </VerticalStackLayout>
                                <HorizontalStackLayout Grid.Column="2" Spacing="8" HorizontalOptions="End">
                                    <Button Text="✓ Accept"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SkillProviderApplicationOfferViewModel}}, Path=ApproveOfferCommand}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="#C8E6C9" TextColor="Black" CornerRadius="10" />
                                    <Button Text="✕ Reject"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SkillProviderApplicationOfferViewModel}}, Path=RejectOfferCommand}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="#FFCDD2" TextColor="Black" CornerRadius="10" />
                                </HorizontalStackLayout>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- SECTION 3: Sent Offers (History) -->
            <Label Text="Sent Applications (History)" FontAttributes="Bold" FontSize="18" />
            <CollectionView ItemsSource="{Binding SentApplicationsHistory}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" Stroke="#D9D9D9" StrokeThickness="1"
                                StrokeShape="RoundRectangle 12" Padding="14" Margin="0,0,0,10">
                            <Grid ColumnDefinitions="3*,3*,2*" VerticalOptions="Center">
                                <VerticalStackLayout>
                                    <Label Text="🔵 Offer" FontSize="10" TextColor="#2196F3" />
                                    <Label Text="{Binding ProjectName}" FontAttributes="Bold">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer
        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SkillProviderApplicationOfferViewModel}}, Path=ViewProjectCommand}" 
                                                 CommandParameter="{Binding .}"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Text="{Binding FormattedRate}" FontSize="12" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1" HorizontalOptions="Center">
                                    <Label Text="{Binding ReceiverName}" FontAttributes="Bold">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer
Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SkillProviderApplicationOfferViewModel}}, Path=ViewProjectOwnerCommand}" 
                                                 CommandParameter="{Binding .}"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Text="{Binding FormattedTimeFrame}" FontSize="12" />
                                </VerticalStackLayout>
                                <Label Grid.Column="2"
                                       Text="{Binding OfferApplicationStatus}"
                                       TextColor="Gray"
                                       FontAttributes="Bold"
                                       VerticalOptions="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- SECTION 4: Received Applications (History) -->
            <Label Text="Received Offers (History)" FontAttributes="Bold" FontSize="18" />
            <CollectionView ItemsSource="{Binding ReceivedOffersHistory}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" Stroke="#D9D9D9" StrokeThickness="1"
                                StrokeShape="RoundRectangle 12" Padding="14" Margin="0,0,0,10">
                            <Grid ColumnDefinitions="3*,3*,2*" VerticalOptions="Center">
                                <VerticalStackLayout>
                                    <Label Text="🟠 Application" FontSize="10" TextColor="#FF9800" />
                                    <Label Text="{Binding ProjectName}" FontAttributes="Bold">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer
        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SkillProviderApplicationOfferViewModel}}, Path=ViewProjectCommand}" 
                                                 CommandParameter="{Binding .}"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Text="{Binding FormattedRate}" FontSize="12" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1" HorizontalOptions="Center">
                                    <Label Text="{Binding SenderName}" FontAttributes="Bold">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer
Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SkillProviderApplicationOfferViewModel}}, Path=ViewProjectOwnerCommand}" 
                                                 CommandParameter="{Binding .}"/>
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Text="{Binding FormattedTimeFrame}" FontSize="12" />
                                </VerticalStackLayout>
                                <Label Grid.Column="2"
                                       Text="{Binding OfferApplicationStatus}"
                                       TextColor="Gray"
                                       FontAttributes="Bold"
                                       VerticalOptions="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
