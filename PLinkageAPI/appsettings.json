{
  "_comment": "API key is omitted for security reasons. To run locally, contact the owner for a key. Otherwise, use the deployed API link hardcoded in the MAUI app.",

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*",

  "ConnectionStrings": {
    "_comment": "MongoDB connection string / API key placeholder",
    "MongoDB": "ADD_KEY_HERE"
  }
}
